<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.user.dao.UserEntityMapper">
    <resultMap id="BaseResultMap" type="com.test.user.entity.UserEntity">
        <id column="ID" jdbcType="INTEGER" property="id" />
        <result column="USER_NAME" jdbcType="VARCHAR" property="userName"/>
        <result column="ACCOUNT" jdbcType="VARCHAR" property="account"/>
        <result column="LOGIN_PASS" jdbcType="VARCHAR" property="loginPass"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 19 16:40:33 CST 2016.
        -->
        ID,USER_NAME,ACCOUNT,LOGIN_PASS
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 19 16:40:33 CST 2016.
        -->
        select
        <include refid="Base_Column_List" />
        from U_USER
    </select>

    <select id="selectByAccount" resultMap="BaseResultMap" parameterType="String">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 19 16:40:33 CST 2016.
        -->
        select
        <include refid="Base_Column_List" />
        from U_USER
        WHERE  ACCOUNT = #{ACCOUNT,jdbcType=VARCHAR}
    </select>

    <select id="selectRolesName" resultType="String" parameterType="String">
        select r.ROLE_NAME from T_ROLE r join T_USER_ROLE ur on r.id = UR.ROLE_ID join U_USER u on ur.user_id = u.id where u.ACCOUNT = #{ACCOUNT,jdbcType=VARCHAR}
    </select>
    
    <select id="selectPermissionName" resultType="String" parameterType="java.util.Set">
        select DISTINCT PERMISSION_NAME from T_PERMISSION p join T_ROLE r on p.ROLE_ID = r.ID where r.ROLE_NAME in
        <foreach collection="set" item="roleName" index="index"
                 open="(" close=")" separator=",">
            #{roleName}
        </foreach>
    </select>

    <select id="selectRecordCount" resultType="Integer" parameterType="com.test.user.dto.UserQueryDto">
        select count(1) from U_USER where 1=1
        <if test="userName != null and userName != ''">
            AND user_name LIKE '%${userName}%'
        </if>
        <if test="account != null and account != ''">
            AND account LIKE '%${account}%'
        </if>
    </select>

    <select id="selectUserList" resultMap="BaseResultMap" parameterType="com.test.user.dto.UserQueryDto">
        SELECT * FROM (select u.id,u.USER_NAME,u.account,ROWNUM rn from U_USER u where 1=1
        <if test="userName != null and userName != ''">
            AND user_name LIKE '%${userName}%'
        </if>
        <if test="account != null and account != ''">
            AND account LIKE '%${account}%'
        </if>
        ) where rn > (#{curPage} - 1) * #{pageSize} and rn &lt;= (#{curPage} - 1) * #{pageSize} + #{pageSize}
    </select>
</mapper>